# UptimeSure

UptimeSure turns API uptime promises into executable guarantees on Rialo.

This repository is being built around Rialo's currently published Venus/REX developer surface and is designed for a Windows-first workflow: edit in VS Code on Windows, build/deploy the Rialo program on an Ubuntu GitHub Actions runner, execute on Rialo DevNet, and host only the frontend on Vercel.
