# UptimeSure V1 Stack

Vercel + Supabase + Base Sepolia. The existing Rialo implementation is retained as an experimental future adapter while the market-ready V1 uses infrastructure that is deployable today.
